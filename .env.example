# Development example only. Never commit real credentials or production connection strings.
# The current master baseline expects a PostgreSQL-compatible connection string.
# Replace every placeholder locally before starting the application.
POSTGRES_PRISMA_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
